language: php

php:
  - 5.6
  - 7.0
  - 7.1
#  - hhvm

before_script: composer update

script:
  - mkdir -p build/logs
  - vendor/bin/phpunit --coverage-clover build/logs/clover.xml

after_success:
  - travis_retry php vendor/satooshi/php-coveralls/bin/php-coveralls
